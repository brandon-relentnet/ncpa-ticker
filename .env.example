# ── Build-time fallbacks (optional) ──────────────────────────────────────────
# Primary configuration should be set via the Admin page (/admin) at runtime.
# These VITE_* variables are only used as fallbacks when server config is empty
# or unreachable.

VITE_NCPA_API_KEY=
# Base host for the official API; the app will append /api when needed.
VITE_NCPA_API_BASE=https://tournaments.ncpaofficial.com
VITE_NCPA_SOCKET_URL=https://tournaments.ncpaofficial.com
VITE_DEFAULT_MATCH_ID=5092

# ── Docker Compose ──────────────────────────────────────────────────────────
# Password for the PostgreSQL database used by the sync server.
# POSTGRES_PASSWORD=ticker
